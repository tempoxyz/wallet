---
tempo-wallet: minor
---

Updated CLI invocation from `tempo-wallet` to `tempo wallet` throughout all documentation, examples, and code. Replaced the standalone install script with `tempo add wallet` and updated the self-update command to delegate to `tempo update wallet`. Added `--skill-sha256` support to the release signing tool and streamlined the release workflow by removing legacy upload paths.
