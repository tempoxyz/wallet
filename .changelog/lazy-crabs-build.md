---
tempo-wallet: minor
---

Renamed the project from `presto` to `tempo-wallet` throughout, updating the binary name, package name, environment variables (`PRESTO_*` → `TEMPO_*`), config/data directories, error types (`PrestoError` → `TempoWalletError`), AI skill files, release workflow, documentation, and all tests.
