---
presto: minor
---

Added support for expiring nonces in charge payments by enabling the `with_expiring_nonces` option on the `TempoProvider` and updating the `mpp` dependency to the `georgios/expiring-nonces` branch.
